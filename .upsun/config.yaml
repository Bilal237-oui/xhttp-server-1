applications:
    app:
        type: nodejs:20
        size: M
        web:
            commands:
                start: node server.js
            locations:
                /:
                    passthru: true

routes:
    "https://{default}/":
        type: upstream
        upstream: "app:http"
    "http://{default}/":
        type: redirect
        to: "https://{default}/"
